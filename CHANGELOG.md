# 更新日志

**简体中文** | [English](CHANGELOG.en.md)

本文件记录本项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增
- **剪贴板历史面板**：按热键（默认 ⌃⇧H，可选 ⌃⌥H / ⌘⇧H）弹出浮动面板，列出最近
  条目并标 1–9；按数字或 ↑↓ + 回车即**直接键入到当前聚焦的输入框**（不动剪贴板），
  ⌘回车则复制到剪贴板；可直接打字过滤；Esc 或点击其他地方关闭。面板不会抢走目标
  应用的焦点。位置四选：**菜单栏图标下方（默认）**、文本光标附近（应用不提供光标
  位置时回退到图标下方，如 VNC / 远程桌面 / 虚拟机）、屏幕中央、**自定义**（呼出后
  直接拖动，松手即保存，可一键重置）。需先在设置中开启剪贴板历史。
- **快捷键自由录制**：两个热键（键入剪贴板、历史面板）不再限于预设，点击快捷键
  按钮后按下任意组合键即可（至少一个 ⌃⌥⇧⌘），被其他软件占用时会明确提示，可一键
  恢复默认。旧版本选择的预设会自动迁移。
- 历史面板可设置显示条数（3–9，默认 9），数字快捷键与之对应，面板高度自适应。

## [0.1.3] - 2026-09-13

### 变更
- **macOS 应用改用稳定的代码签名证书**：签名要求不再随每次构建变化，因此**应用内更新
  后辅助功能权限保持有效，不再需要重新授权**。从 0.1.2 及更早（ad-hoc 签名）版本
  升级时仍需重新授权一次（删除条目再添加），之后永久有效。更新助手不再重置权限记录。
  应用尚未经 Apple 公证，Gatekeeper 提示照旧。
- 主窗口和设置窗口可以自由缩小（最小约 440×360），内容超出时表单自行滚动；
  之前窗口被强制撑到内容全高、无法缩小。默认尺寸 480×640。

### 新增
- **剪贴板历史（基础版，默认关闭）**：在设置中开启后，macOS 应用会记录你复制的
  文本（去重、可设上限 20/50/100 条，单条最大 100 KB），只保存在本机的
  Application Support 文件夹（权限 0600）；密码管理器标记为隐藏/临时的内容不记录。
  菜单栏「剪贴板历史」子菜单列出最近条目，点选即放回剪贴板，再用热键输入。
  完整的历史界面形态（弹窗面板等）将在后续版本确定。
- CLI：`--stdin`，从标准输入读取要输入的文本，代替剪贴板（供应用直接键入历史
  条目，内容不进入进程参数）。

## [0.1.2] - 2026-09-11

### 修复
- **权限状态显示不更新**：`AXIsProcessTrusted()` 在进程内会被缓存，运行中的应用
  永远看不到权限的授予或撤销——用户明明已经授权，应用却一直提示"需要辅助功能
  权限"。现在通过启动一个新进程（内置引擎的 `--check-permission`）来查询，
  授权后 2 秒内界面即更新，无需重启应用。
- **更新后开关开着却不起作用**：应用内更新会替换整个应用，macOS 把旧的授权记录绑定
  在旧版本的签名上，开关关掉再打开也没用，必须删除条目后重新添加（用户反馈中
  发现）。更新后若 12 秒内仍无权限，应用会弹出明确的 − / + 操作步骤，并提供
  「帮我删除条目」按钮；设置窗口的权限区也常驻这段说明。
- 打开系统设置的深链接改为按顺序尝试多个 URL 方案，兼容不同 macOS 版本的
  设置应用。

### 新增
- CLI：`--check-permission`，以退出码返回是否允许模拟键盘输入（0 = 允许），不做
  任何输入；供 macOS 应用查询权限状态。

## [0.1.1] - 2026-09-11

### 新增
- **`--mode keycode`（远程控制台模式）**：按当前键盘布局逐字符按下真实键码
  （需要时自动带 Shift / Option），而不是发送 Unicode 文本事件。修复了 VNC、
  远程控制台、虚拟机窗口中**每个字符都被输入成 `a`** 的问题——这些环境只转发
  物理键码、忽略附加的 Unicode 文本。发送期间会临时切换到 ASCII 输入源，避免
  输入法截获按键，结束后恢复原输入法。键盘布局上没有的字符（中日文等）回退为
  Unicode 方式并给出一次警告。
- macOS 应用（设置窗口 + 菜单栏菜单）和托盘菜单中新增「远程控制台模式
  （VNC / 虚拟机）」开关，与其他设置一同持久化。
- **macOS 应用内更新**：启动时和每 24 小时检查一次 GitHub Releases（可关闭），
  显示更新说明，下载通用版应用压缩包并校验 SHA-256，然后替换应用并重新启动。
  菜单栏菜单和设置窗口均提供「检查更新…」。
- 应用图标。
- 应用内显示版本号（菜单栏标题行、主窗口头部、设置窗口）。

## [0.1.0] - 2026-08-07

首个正式版本。已在 macOS 上完整验证（单次、热键、托盘、原生应用四种模式）；
Windows 与 Linux 版本已通过 CI 构建，运行时验证待补。

### 新增
- 通过 `arboard` 读取剪贴板文本；剪贴板为空或非文本时优雅退出。
- 通过 `enigo` 发送键盘输入：`--interval` 为 0 时走批量快速模式，否则按可配置
  间隔逐字符发送。
- 换行与 Tab 作为真实的 Return / Tab 按键发送，提升目标应用兼容性。
- 输入前将换行统一为 LF（来自 Windows 剪贴板的 CRLF / CR 不再产生双倍换行）。
- `--speed fast|normal|slow` 打字速度预设，作为 `--interval` 的友好替代。
- **常驻热键模式**（`--features hotkey`）：`cliptype --hotkey [组合键]` 常驻运行，
  每次按下热键就输入当前剪贴板内容（默认 `ctrl+shift+v`）。会等待热键的修饰键
  全部松开后再输入，组合键本身不会污染输出。
- **状态栏 / 托盘模式**（`--features tray`，macOS 和 Windows）：`cliptype --tray`
  显示原生状态栏图标，包含当前热键、暂停 / 恢复、打字速度预设。速度变更会写入
  配置文件并在下次启动时恢复。每个平台的二进制只包含自己平台的 UI 代码；
  Linux 构建不含托盘。
- **macOS 原生应用**：主窗口 + 常驻菜单栏，界面支持中文 / 英文 / 日文三语。
- macOS 上未授予辅助功能权限时，程序会提前检测并给出清晰的引导后退出，而不是
  看似成功、实际每个按键都被系统静默丢弃。
- 发布流程：打 tag 自动构建并附加预编译产物——macOS 应用（通用版）以及 macOS
  （Apple Silicon 和 Intel）、Windows、Linux 的 CLI，每个都带 SHA-256 校验文件。
- 跨平台 CI（macOS / Windows / Linux）：fmt、clippy、构建、测试。

### 变更
- 所有面向用户的 CLI 提示和 `--help` 文本统一为英文。

### 修复
- 进程在最后一批键盘事件送达前退出会导致尾部字符丢失；现在退出前会短暂等待。
- 逐字符模式（`--interval > 0`）会被输入法截获，导致中日文乱码、emoji 丢失；
  现在改用与快速模式相同的、不受输入法影响的事件机制。
